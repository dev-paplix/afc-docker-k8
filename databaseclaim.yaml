apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: databaseclaim
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: datastorage